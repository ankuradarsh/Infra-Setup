name: jenkins
image: jenkins/jenkins:jdk21

replicas: 1

pvcStorage: 2Gi

requests:
    memory: "700Mi"
    cpu: "250m"
limits:
    memory: "1Gi"
    cpu: "500m"

portNumber: 8080
nodePortNumber: 30080
tunnelPortNumber: 50000

volumeMounts:
- name:  jenkins-pvc
  mountPath:  /var/jenkins_home

volumes:
- name: jenkins-pvc
persistentVolumeClaim:
    claimName: jenkins-pvc