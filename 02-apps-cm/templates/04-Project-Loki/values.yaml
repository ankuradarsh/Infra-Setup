backend:
  replicas: 1

ui:
  enabled: true

read:
  replicas: 1

write:
  replicas: 1


# FOR one or more objects failed to apply, reason: CustomResourceDefinition.apiextensions.k8s.io "grafanaagents.monitoring.grafana.com" is invalid: metadata.annotations: Too long: may not be more than 262144 bytes
monitoring:
  selfMonitoring:
    enabled: false
    grafanaAgent:
      installOperator: false