apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "https://localhost:30082"
      path: "global-credentials" # KV engine path
      version: "v1"
      auth:
        tokenSecretRef:
          namespace: observability
          name: vault-token # Kubernetes Secret containing the token
          key: token
