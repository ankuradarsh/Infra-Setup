# The elasticsearch-credentials secret is managed by the Kibana chart's
# pre-install hook (external-secret.yaml) so it is available to the
# token-creation Job before the Kibana release installs.
# Nothing to provision here for Elasticsearch itself.
