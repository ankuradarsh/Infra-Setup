# The elasticsearch-credentials secret is owned by the Elasticsearch chart
# (03-Elasticsearch/templates/externalsecret.yaml) as a permanent manifest.
# The Kibana pre-install Job's wait-for-credentials initContainer polls for
# the secret to be ready before proceeding — no hook needed here.
