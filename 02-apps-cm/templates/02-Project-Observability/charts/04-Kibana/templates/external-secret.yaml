# apiVersion: external-secrets.io/v1
# kind: ExternalSecret
# metadata:
#   name: kibana-es-token
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: kibana-es-token   # MUST match Deployment
#     creationPolicy: Owner
#   data:
#     - secretKey: token      # MUST match Deployment key
#       remoteRef:
#         key: elastisearch
#         property: service-account-token