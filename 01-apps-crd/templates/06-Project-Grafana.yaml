apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.Application.5.name }}
  namespace: {{ .Values.Application.5.namespace }}
spec:
  project: {{ .Values.project }}
  syncPolicy:
    automated:
      prune: {{ .Values.prune }}
      selfHeal: {{ .Values.selfHeal }}
    syncOptions:
      - CreateNamespace=true
  destination:
    namespace: {{ .Values.Application.5.namespace }}
    server: {{ .Values.destinationServer}}
  source:
    repoURL: {{ .Values.Application.5.projectSource.url }}
    path: {{ .Values.Application.5.projectSource.path }}